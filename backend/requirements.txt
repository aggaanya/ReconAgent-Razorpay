fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.6

# Database infrastructure (Phase 2)
sqlalchemy>=2.0,<3.0
psycopg2-binary>=2.9
alembic>=1.13

# AI layer — centralized LLM access via app.ai.llm (OpenAI-compatible API)
openai>=2.0,<3.0

# AI layer — LangGraph orchestration over app.ai.tools (Phase 3)
langgraph>=1.0,<2.0

pytest>=8.0
httpx>=0.27

# data/

Holds ReconAgent datasets.

**Planned contents (Phase 3):**

- `generator/generate_synthetic_data.py` — programmatic generator for the labeled synthetic dataset
- `payment_settlement.csv`, `bank_statement.csv`, `internal_ledger.csv` — 50+ records covering every exception scenario
- internal ground-truth mapping (excluded from version control via `.gitignore`: `data/ground_truth/`)

Currently empty by design — no fake or sample data is committed ahead of Phase 3.
